controllers:
  main:
    initContainers:
      migrate:
        image:
          repository: ghcr.io/toeverything/affine-self-hosted
          tag: "stable"
          pullPolicy: IfNotPresent
        command:
        - sh
        - -c
        - node ./scripts/self-host-predeploy.js
        env:
        - name: AFFINE_CONFIG_PATH
          value: /root/.affine/config
        - name: NODE_ENV
          value: production
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: affine-secrets
              key: database-url
        - name: REDIS_SERVER_HOST
          value: redis-affine-master.private.svc.cluster.local
        - name: REDIS_SERVER_PORT
          value: "6379"
        - name: REDIS_SERVER_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-affine
              key: redis-password
        - name: AFFINE_INDEXER_ENABLED
          value: "false"

    containers:
      main:
        image:
          repository: ghcr.io/toeverything/affine-self-hosted
          tag: "stable"
          pullPolicy: IfNotPresent
        env:
        - name: AFFINE_CONFIG_PATH
          value: /root/.affine/config
        - name: NODE_ENV
          value: production
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: affine-secrets
              key: database-url
        - name: REDIS_SERVER_HOST
          value: redis-affine-master.private.svc.cluster.local
        - name: REDIS_SERVER_PORT
          value: "6379"
        - name: REDIS_SERVER_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-affine
              key: redis-password
        - name: AFFINE_SERVER_HOST
          value: notes.mysweetpea.cc
        - name: AFFINE_SERVER_HTTPS
          value: "true"
        - name: AFFINE_INDEXER_ENABLED
          value: "false"
        ports:
        - containerPort: 3010
          name: http
        probes:
          liveness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /api/ready
                port: 3010
              initialDelaySeconds: 30
          readiness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /api/ready
                port: 3010
              initialDelaySeconds: 10

service:
  main:
    ports:
      http:
        port: 3010

ingress:
  main:
    annotations:
      traefik.ingress.kubernetes.io/router.entrypoints: web
    hosts:
    - host: notes.mysweetpea.cc
      paths:
      - path: /
        pathType: Prefix
        service:
          identifier: main
          port: http

persistence:
  config:
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 5Gi
    storageClass: longhorn
    globalMounts:
    - path: /root/.affine/config
  uploads:
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 10Gi
    storageClass: longhorn
    globalMounts:
    - path: /root/.affine/storage
