controllers:
  main:
    containers:
      main:
        image:
          repository: ghcr.io/hotio/qbittorrent
          tag: latest
    type: deployment
service:
  main:
    controller: main
    ports:
      http:
        port: 8080
