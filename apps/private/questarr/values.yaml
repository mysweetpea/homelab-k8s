controllers:
  main:
    containers:
      main:
        image:
          repository: ghcr.io/doezer/questarr
          tag: latest
    type: deployment
persistence:
  data:
    existingClaim: questarr-data-lh
    mountPath: /app/data
service:
  main:
    controller: main
    ports:
      http:
        port: 5000
