controllers:
  main:
    containers:
      main:
        image:
          repository: ghcr.io/containers/kubernetes-mcp-server
          tag: v0.0.66
    type: deployment
service:
  main:
    controller: main
    ports:
      http:
        port: 8080
