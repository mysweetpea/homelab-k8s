controllers:
  main:
    containers:
      main:
        image:
          repository: ghcr.io/hotio/sonarr
          tag: v4-4.0.0.748
    type: deployment
persistence:
  config:
    existingClaim: sonarr-lh
  media:
    enabled: true
    type: persistentVolumeClaim
    existingClaim: media-storage-lh
    globalMounts:
      - path: /data
service:
  main:
    controller: main
    ports:
      http:
        port: 8989
    type: LoadBalancer
    loadBalancerIP: 192.168.20.219
    allocateLoadBalancerNodePorts: false
