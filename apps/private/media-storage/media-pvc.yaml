apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-storage-lh
  namespace: private
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Gi
