controllers:
  main:
    containers:
      main:
        image:
          repository: ghcr.io/nzbdav-dev/nzbdav
          tag: 0.6.4
        ports:
          - name: http
            containerPort: 8080
            protocol: TCP
    type: deployment
persistence:
  data:
    existingClaim: nzbdav-data-lh
service:
  main:
    controller: main
    type: LoadBalancer
    loadBalancerIP: 192.168.20.212
    ports:
      http:
        port: 8080
        targetPort: 8080
