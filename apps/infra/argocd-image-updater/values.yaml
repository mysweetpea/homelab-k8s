config:
  argocd:
    serverAddress: argocd-server.argocd.svc.cluster.local
    insecure: true
  log:
    level: info
  git:
    user: "argocd-image-updater"
    email: "argocd-image-updater@mysweetpea.cc"
  registries:
    - name: GHCR
      prefix: ghcr.io
      api_url: https://ghcr.io
      credentials: pullsecret:argocd/ghcr-credentials
  sshConfig:
    config: |
      Host github.com
        IdentityFile /app/ssh-keys/id_rsa
        UserKnownHostsFile /app/config/ssh/ssh_known_hosts
extraArgs:
  - --registries-conf-path
  - /app/config/registries.conf
# ImageUpdater CR — v1.2.1 controller is CR-driven.
# CRITICAL (Session 41, Aug 14 2026): each app needs BOTH
#   - images[].manifestTargets.helm.tag = <values.yaml tag path>  (where to write)
#   - writeBackConfig.gitConfig.writeBackTarget = helmvalues:<values.yaml path> (which file)
# CRITICAL #2 (Session 43, Aug 14 2026): the CRD DEFAULTS writeBackConfig.method
# to "argocd" (live-spec-only write-back) at BOTH levels. Per-app method
# OVERRIDES the top-level spec.writeBackConfig.method in mergeWBCSettings →
# WITHOUT an explicit per-app `method: git:secret:argocd/ssh-git-creds`, the
# controller only updates the live spec and NEVER commits to git (the
# "Committing N parameter update(s)" log followed by no commits in origin/main).
# The earlier "proven" state had manifestTargets but NO writeBackTarget → the
# controller wrote .argocd-source-*.yaml overrides, which ArgoCD IGNORES for
# multi-source apps (sources[] + $values) → commits landed in git but no
# rollouts ever happened (live images stayed old: gotify 2.6.1, n8n 2.33.6).
# The SSH key secret must be named `ssh-git-creds` (chart default mount
# /app/ssh-keys/id_rsa, subPath sshPrivateKey) — the old name
# `argocd-image-updater-git-ssh` was never mounted → silent fallback to
# live-spec updates (reverted by selfHeal).
extraObjects:
  - |
    apiVersion: argocd-image-updater.argoproj.io/v1alpha1
    kind: ImageUpdater
    metadata:
      name: homelab-image-updater
      namespace: argocd
    spec:
      writeBackConfig:
        method: git:secret:argocd/ssh-git-creds
        gitConfig:
          repository: git@github.com:mysweetpea/homelab-k8s.git
          branch: main
      applicationRefs:
        - namePattern: n8n
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/n8n/values.yaml
          images:
            - alias: n8n
              imageName: n8nio/n8n
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^2\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: immich
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/immich/values.yaml
          images:
            - alias: immich
              imageName: ghcr.io/immich-app/immich-server
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: homepage
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/monitoring/homepage/values.yaml
          images:
            - alias: homepage
              imageName: ghcr.io/gethomepage/homepage
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: jellyfin
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/jellyfin/values.yaml
          images:
            - alias: jellyfin
              imageName: jellyfin/jellyfin
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: authentik
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/dmz/authentik/values.yaml
          images:
            - alias: authentik
              imageName: ghcr.io/goauthentik/server
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: authentik.image.repository
                  tag: authentik.image.tag
        - namePattern: nextcloud
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/nextcloud/values.yaml
          images:
            - alias: nextcloud
              imageName: library/nextcloud
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: image.repository
                  tag: image.tag
        - namePattern: vaultwarden
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/dmz/vaultwarden/values.yaml
          images:
            - alias: vaultwarden
              imageName: vaultwarden/server
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: open-webui
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/open-webui/values.yaml
          images:
            - alias: open-webui
              imageName: ghcr.io/open-webui/open-webui
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: cloudflared
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/dmz/cloudflared/values.yaml
          images:
            - alias: cloudflared
              imageName: cloudflare/cloudflared
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^\\d{4}\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: gotify
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/gotify/values.yaml
          images:
            - alias: gotify
              imageName: gotify/server
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: seerr
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/dmz/seerr/values.yaml
          images:
            - alias: seerr
              imageName: ghcr.io/seerr-team/seerr
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: sonarr
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/sonarr/values.yaml
          images:
            - alias: sonarr
              imageName: ghcr.io/hotio/sonarr
              commonUpdateSettings:
                updateStrategy: newest-build
                allowTags: "regexp:^v4-\\d+\\.\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: radarr
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/radarr/values.yaml
          images:
            - alias: radarr
              imageName: ghcr.io/hotio/radarr
              commonUpdateSettings:
                updateStrategy: newest-build
                allowTags: "regexp:^release-\\d+\\.\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: prowlarr
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/prowlarr/values.yaml
          images:
            - alias: prowlarr
              imageName: ghcr.io/hotio/prowlarr
              commonUpdateSettings:
                updateStrategy: newest-build
                allowTags: "regexp:^release-\\d+\\.\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: qdrant
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/qdrant/values.yaml
          images:
            - alias: qdrant
              imageName: docker.io/qdrant/qdrant
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: image.repository
                  tag: image.tag
        - namePattern: flaresolverr
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/flaresolverr/values.yaml
          images:
            - alias: flaresolverr
              imageName: ghcr.io/flaresolverr/flaresolverr
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: rustdesk
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/rustdesk/values.yaml
          images:
            - alias: rustdesk-hbbs
              imageName: rustdesk/rustdesk-server
              commonUpdateSettings:
                updateStrategy: newest-build
                allowTags: "regexp:^1\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.hbbs.containers.main.image.repository
                  tag: controllers.hbbs.containers.main.image.tag
            - alias: rustdesk-hbbr
              imageName: rustdesk/rustdesk-server
              commonUpdateSettings:
                updateStrategy: newest-build
                allowTags: "regexp:^1\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.hbbr.containers.main.image.repository
                  tag: controllers.hbbr.containers.main.image.tag
        - namePattern: uptime-kuma
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/monitoring/uptime-kuma/values.yaml
          images:
            - alias: uptime-kuma
              imageName: louislam/uptime-kuma
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: image.repository
                  tag: image.tag
        - namePattern: aiostreams
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/aiostreams/values.yaml
          images:
            - alias: aiostreams
              imageName: ghcr.io/viren070/aiostreams
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: affine
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/dmz/affine/values.yaml
          images:
            - alias: affine
              imageName: ghcr.io/toeverything/affine
              commonUpdateSettings:
                updateStrategy: newest-build
                allowTags: "regexp:^stable-[a-f0-9]+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
            - alias: affine-migrate
              imageName: ghcr.io/toeverything/affine
              commonUpdateSettings:
                updateStrategy: newest-build
                allowTags: "regexp:^stable-[a-f0-9]+$"
              manifestTargets:
                helm:
                  name: controllers.main.initContainers.migrate.image.repository
                  tag: controllers.main.initContainers.migrate.image.tag
        - namePattern: bazarr
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/bazarr/values.yaml
          images:
            - alias: bazarr
              imageName: ghcr.io/hotio/bazarr
              commonUpdateSettings:
                updateStrategy: newest-build
                allowTags: "regexp:^release-\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: qbittorrent
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/qbittorrent/values.yaml
          images:
            - alias: qbittorrent
              imageName: ghcr.io/hotio/qbittorrent
              commonUpdateSettings:
                updateStrategy: newest-build
                allowTags: "regexp:^release-\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: element-web
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/dmz/element-web/values.yaml
          images:
            - alias: element-web
              imageName: ghcr.io/element-hq/element-web
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: image.repository
                  tag: image.tag
        - namePattern: grafana
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/monitoring/grafana/values.yaml
          images:
            - alias: grafana
              imageName: grafana/grafana
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: image.repository
                  tag: image.tag
        - namePattern: koalasync
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/dmz/koalasync/values.yaml
          images:
            - alias: koalasync
              imageName: ghcr.io/shik3i/koalasync
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: mcp-server
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/mcp-server/values.yaml
          images:
            - alias: mcp-server
              imageName: ghcr.io/containers/kubernetes-mcp-server
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: netdata
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/monitoring/netdata/values.yaml
          images:
            - alias: netdata
              imageName: netdata/netdata
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: image.repository
                  tag: image.tag
        - namePattern: nzbdav
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/nzbdav/values.yaml
          images:
            - alias: nzbdav
              imageName: ghcr.io/nzbdav-dev/nzbdav
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: ollama
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/dmz/ollama/values.yaml
          images:
            - alias: ollama
              imageName: ollama/ollama
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: openclaw
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/openclaw/values.yaml
          images:
            - alias: openclaw
              imageName: ghcr.io/openclaw/openclaw
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^20\\d{2}\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: questarr
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/private/questarr/values.yaml
          images:
            - alias: questarr
              imageName: ghcr.io/doezer/questarr
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: searxng
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/dmz/searxng/values.yaml
          images:
            - alias: searxng
              imageName: searxng/searxng
              commonUpdateSettings:
                updateStrategy: newest-build
                allowTags: "regexp:^20\\d{2}\\.\\d+\\.\\d+.*$"
              manifestTargets:
                helm:
                  name: controllers.main.containers.main.image.repository
                  tag: controllers.main.containers.main.image.tag
        - namePattern: matrix-synapse
          writeBackConfig:
            method: git:secret:argocd/ssh-git-creds
            gitConfig:
              writeBackTarget: helmvalues:apps/dmz/matrix-synapse/values.yaml
          images:
            - alias: matrix-synapse
              imageName: ghcr.io/element-hq/synapse
              commonUpdateSettings:
                updateStrategy: semver
                allowTags: "regexp:^v\\d+\\.\\d+\\.\\d+$"
              manifestTargets:
                helm:
                  name: image.repository
                  tag: image.tag
resources:
  limits:
    cpu: 500m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
